import React, {Component} from 'react';
import {withRouter} from 'react-router-dom';

class LineMap extends React.Component {

    render(){
        return (
            <div> </div>
        )
    }
}
export default withRouter(LineMap);